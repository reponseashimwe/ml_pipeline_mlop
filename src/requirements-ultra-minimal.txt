# Ultra-minimal requirements for 512MB limit
setuptools>=68.0.0
wheel>=0.40.0

# Core API only
fastapi
uvicorn[standard]
python-multipart
pydantic

# Core ML (CPU only)
tensorflow-cpu
numpy
Pillow

# Essential monitoring
psutil 